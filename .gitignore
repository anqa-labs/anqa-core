.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# vendored percolator builds into the workspace target dir
vendor/percolator/target

# Demo venue keys and mints (devnet play money)
app/.demo-*.json

# Keeper and feed-pusher logs from the local demo venue
app/.keeper-*.log
app/.push-feed.log
